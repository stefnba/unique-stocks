SELECT
    mic,
    operating_mic,
    mic_type,
    mic_name AS exchange_name,
    legal_entity_name,
    lei,
    market_category_code,
    acronym,
    country_iso2,
    city,
    website,
    status,
    creation_date,
    last_update_date,
    last_validation_date,
    expiry_date,
    comments,
    is_operating_mic,
    is_active,
    data_provider AS registry_provider,
    snapshot_date AS registry_snapshot_date,
    row_hash AS registry_row_hash,
    source_uri AS registry_source_uri,
    ingested_at AS registry_ingested_at
FROM {{ ref('stg_exchange_mic_registry') }}
