"""Non-secret AWS resource defaults for the pipelines app."""

# AWS region and bucket name
DEFAULT_REGION = "eu-central-1"
DEFAULT_BUCKET_NAME = "unique-stocks-dev"

# IAM user and policy names
DEFAULT_IAM_USER = "unique-stocks-pipelines"
DEFAULT_INLINE_POLICY_NAME = "unique-stocks-pipelines-s3-landing"


