# Base service definitions — no environment-specific values.
# Never use directly. Compose with an override:
#   docker compose -f .../docker-compose.yml -f .../docker-compose.dev.yml ...
# Use make infra-* targets from the repo root.

name: unique-stocks

services:

  # ── Prefect server (API + UI) ──────────────────────────────────────────────
  prefect-server:
    image: prefecthq/prefect:3.7.1
    command: prefect server start --host 0.0.0.0
    environment:
      PREFECT_SERVER_DATABASE_CONNECTION_URL: postgresql+asyncpg://prefect:prefect@postgres:5432/prefect
    depends_on:
      postgres:
        condition: service_healthy

  # ── Postgres (Prefect persistence) ────────────────────────────────────────
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: prefect
      POSTGRES_PASSWORD: prefect
      POSTGRES_DB: prefect
    volumes:
      - prefect-db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U prefect"]
      interval: 5s
      timeout: 5s
      retries: 10

  # ── Pipelines worker ───────────────────────────────────────────────────────
  pipelines:
    build:
      context: ..
      dockerfile: Dockerfile
    environment:
      PREFECT_API_URL: http://prefect-server:4200/api
      PREFECT_WORK_DIR: /app
    depends_on:
      - prefect-server
    command: uv run prefect worker start --pool default-process-pool

volumes:
  prefect-db:
