"""Generic operational health helpers.

This package contains reusable health-check primitives for the pipelines
runtime. Concrete local smoke presets live in ``orchestration.smoke`` because
they import app domains and run real domain flows.
"""
